package net.sourceforge.pinemup.ui.swing.tray.fallback;

import javax.swing.*;

public class FallbackDialog extends JFrame {
   public FallbackDialog(String title) {
      super(title);

      setSize(32, 32);
   }
}
